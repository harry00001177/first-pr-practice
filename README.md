# first-pr-practice

A tiny greeting utility, used as a sandbox for practicing the GitHub pull request workflow.

## Usage

```bash
python greet.py Ada
# Hello, Ada!
```

## API

`greet(name)` returns a friendly greeting string. If no name is given, it falls back to `"Hello, stranger!"`.

## Tests

```bash
python -m unittest discover tests
```
